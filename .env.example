# RevenueCat Web Billing public API key (test store).
# Public/publishable by design — safe to ship to the browser, same model as a Stripe pk_ key.
NEXT_PUBLIC_RC_API_KEY=rcb_your_public_api_key_here
